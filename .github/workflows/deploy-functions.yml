name: Deploy Firebase Functions

on:
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow registration
        run: echo "BXH ARENA Firebase Functions workflow is registered."
